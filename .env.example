# EVVM Story Aeneid
STORY_RPC_URL=https://aeneid.storyrpc.io
ADMIN_PRIVATE_KEY=0x
USDC_K_ADDRESS=0xd35890acdf3BFFd445C2c7fC57231bDE5cAFbde5
# IP token (optional). Aeneid WIP default: 0x1514000000000000000000000000000000000000
IP_TOKEN_ADDRESS=
# Story Protocol v4 API (optional; for IP asset count on dashboard). Get key at https://story.foundation
STORY_API_KEY=
EVVM_CORE_ADDRESS=0xa6a02E8e17b819328DDB16A0ad31dD83Dd14BA3b
EVVM_X402_ADAPTER_ADDRESS=0xDf5eaED856c2f8f6930d5F3A5BCE5b5d7E4C73cc

# KrumpVerify contracts (Story Aeneid)
KRUMP_VERIFY_ADDRESS=0x41CE400d0C0f8d5c38BDf68970981b359cB5bb4A
KRUMP_TREASURY_ADDRESS=

# x402 relayer (Krump Verify relayer; optional)
X402_RELAYER_URL=http://localhost:7350

# Agent wallets (HEX private keys; keep secret)
VERIFIER_PRIVATE_KEY=0x 
CHOREOGRAPHER_PRIVATE_KEY=0x
MINER_PRIVATE_KEY=0x
TREASURY_PRIVATE_KEY=0x

# Memory and API
MEMORY_DIR=./memory
API_PORT=8081

# Optional: Minecraft mod (Paper plugin API URL)
MINECRAFT_SERVER_HOST=localhost
MINECRAFT_RCON_PORT=25575
MINECRAFT_RCON_PASSWORD=

# Optional: Mineflayer bots (same process as swarm; use server in offline mode)
# When set, bots connect to the Paper server and sync in-game position to agent markers.
MINECRAFT_HOST=localhost
MINECRAFT_PORT=25565
# Max Mineflayer bots to connect (default 1 to reduce timeouts). Set to 2 or 4 for more.
# MINECRAFT_BOT_LIMIT=1
# Comma-separated agent ids to spawn bots for (e.g. verifier_001,choreographer_001). Unset = one bot per swarm agent.
# MINECRAFT_BOT_AGENTS=

# Optional: LLM-driven decisions (OpenRouter). When set with MINECRAFT_HOST, bots take autonomous actions (chat, commission, pay).
OPENROUTER_API_KEY=
# Model (default: stepfun/step-3.5-flash:free)
# LLM_MODEL=stepfun/step-3.5-flash:free
# Decision interval in ms (default: 45000)
# LLM_DECISION_INTERVAL_MS=45000
